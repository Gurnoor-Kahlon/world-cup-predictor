# Core data + numerics
pandas>=2.0
numpy>=1.24
scipy>=1.10

# Machine learning
scikit-learn>=1.3

# Web app + charts
streamlit>=1.30
plotly>=5.18
matplotlib>=3.7

# Testing + linting
pytest>=7.4
ruff>=0.5

# --- Optional FastAPI backend (api/main.py) ---
# Only needed if you run the REST API; the Streamlit app and CLI don't require it.
# fastapi>=0.110
# uvicorn>=0.27
# httpx>=0.26

# --- Optional gradient-boosting backends ---
# The model uses scikit-learn's GradientBoosting by default and works without
# these. Install one to enable a stronger classifier (see src/model.py):
# xgboost>=2.0
# lightgbm>=4.0
